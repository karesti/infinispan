package org.infinispan.api.collection.listener;

import org.infinispan.api.Experimental;
import org.infinispan.api.collection.KeyValueStore;

/**
 * KeyValueStoreListener interface to be used with {@link KeyValueStore}
 * Listeners, Client or Embedded, must implement this interface
 *
 * @since 10.0
 * @author Katia Aresti, karesti@redhat.com
 */
@Experimental
public interface KeyValueStoreListener {
}
